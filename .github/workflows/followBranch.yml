name: followBranch
on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-18.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@master
      - name: git setting
        run: |
          git config --local user.email "hirakue388@hoge.com"
          git config --local user.name "hirakue388"
      - name: checkout-follow-branch
        run: git checkout follow_1
      - name: pull-branch
        run: git pull origin follow_1
      - name: merge master
        run: git merge master
      - name: push follow_1 branch
        run: git push origin follow_1
        